import PropTypes from "prop-types";

const Homepage = () => {
  return (
    <>
      <h1>test</h1>

      {/* <button
        type="button"
        onClick={() =>
          i18n.changeLanguage(i18n.language === "ko" ? "en" : "ko")
        }
      >
        {t("test")}
      </button> */}
    </>
  );
};

export default Homepage;
